# Super Minimal Requirements - Guaranteed Render.com Compatible
# Older stable versions - no compilation issues

# Core Flask (older stable versions)
flask==2.3.3
flask-sqlalchemy==3.0.5
flask-cors==4.0.0
Werkzeug==2.3.7
SQLAlchemy==1.4.53

# Database driver (pure Python)
pg8000==1.29.8

# JWT (pure Python)
PyJWT==2.8.0

# Production server
gunicorn==21.2.0

# Environment
python-dotenv==1.0.0 