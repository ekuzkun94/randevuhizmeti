# Ultra Minimal Requirements for Render.com (Guaranteed No Compilation)
# Only core packages - no extra features initially

# Core Flask only
flask==3.0.0
flask-sqlalchemy==3.1.1
flask-cors==4.0.0
Werkzeug==3.0.1
SQLAlchemy==2.0.20

# Database driver (pure Python)
pg8000==1.30.2

# Basic JWT (no cryptography)
PyJWT==2.8.0

# Production server
gunicorn==21.2.0

# Environment handling
python-dotenv==1.0.0 